import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Habit Tracker' };

export default function HabitsPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Habit Tracker — coming in Step 6</h1>
    </main>
  );
}
