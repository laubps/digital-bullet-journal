import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Mood Tracker' };

export default function MoodPage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Mood Tracker — coming in Step 5</h1>
    </main>
  );
}
